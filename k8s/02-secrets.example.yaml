# TEMPLATE ONLY - never commit real secrets.
# Create the real secret with:
#   kubectl -n clinicos create secret generic clinicos-secrets \
#     --from-literal=DB_PASSWORD='<strong-password>' \
#     --from-literal=JWT_SECRET='<64+ char secret>' \
#     --from-literal=MAIL_USERNAME='<smtp user>' \
#     --from-literal=MAIL_PASSWORD='<smtp password>' \
#     --from-literal=GOOGLE_CLIENT_ID='<oauth client id>' \
#     --from-literal=GOOGLE_CLIENT_SECRET='<oauth client secret>'
apiVersion: v1
kind: Secret
metadata:
  name: clinicos-secrets
  namespace: clinicos
type: Opaque
stringData:
  DB_PASSWORD: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME_MINIMUM_64_CHARACTERS_FOR_HS512_ALGORITHM_SECURITY_REQ"
  MAIL_USERNAME: ""
  MAIL_PASSWORD: ""
  GOOGLE_CLIENT_ID: ""
  GOOGLE_CLIENT_SECRET: ""

